name: Validate Codex plugin

on:
  pull_request:
    paths:
      - ".agents/plugins/**"
      - ".codex-plugin/**"
      - ".mcp.json"
      - "docs/codex-plugin.md"
      - ".github/workflows/codex-plugin-validate.yml"
      - "pyproject.toml"
      - "uv.lock"
      - "src/**"
      - "tests/test_codex_plugin.py"
      - "tests/adapters/test_mcp_server.py"
  push:
    branches:
      - main
    paths:
      - ".agents/plugins/**"
      - ".codex-plugin/**"
      - ".mcp.json"
      - "docs/codex-plugin.md"
      - ".github/workflows/codex-plugin-validate.yml"
      - "pyproject.toml"
      - "uv.lock"
      - "src/**"
      - "tests/test_codex_plugin.py"
      - "tests/adapters/test_mcp_server.py"

permissions: read-all

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2

      - name: Validate plugin contracts
        run: uv run --locked pytest -q tests/test_codex_plugin.py tests/adapters/test_mcp_server.py

      - name: Verify local MCP entry point
        run: uv run --locked people-context-mcp --help
